
git config --global user.email "aziz.bagsiraj@gmail.com"
git config --global user.name "Aziz Bagsiraj"

Learn git Part -1

1) git init
to start git 

2) git add [filename] || -A
to copy files on staging area.

3) git status
4) git commit -m "msg"
5) git remote add origin repoName
6) git push origin master
7) git clone repoName
8) git pull origian master
